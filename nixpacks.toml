# nixpacks.toml
[phases.setup]
nixPkgs = ["python311", "nodejs_20", "npm"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"